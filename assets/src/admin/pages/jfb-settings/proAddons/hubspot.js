import IsPROIcon from '../IsPROIcon';

const { __ } = wp.i18n;

export default {
	title: __( 'HubSpot API', 'jet-form-builder' ),
	component: {
		name: 'hubspot',
	},
	disabled: true,
	icon: IsPROIcon,
};