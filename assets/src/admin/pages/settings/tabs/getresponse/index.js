import GetResponseTab from './GetResponseTab.vue';

const { __ } = wp.i18n;

const title = __( 'GetResponse API', 'jet-form-builder' );
const component = GetResponseTab;

export {
	title,
	component
}