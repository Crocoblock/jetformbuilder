import IsPROIcon from '../IsPROIcon';

const { __ } = wp.i18n;

export default {
	title: __( 'Stripe Gateway API', 'jet-form-builder' ),
	component: {
		name: 'stripe'
	},
	disabled: true,
	icon: IsPROIcon,
}