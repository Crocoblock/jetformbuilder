
import * as loading from '../loading'
import * as query from '../query'
import * as table from '../table'
import * as chooseColumn from '../choose-column'

export default {
	strict: true,
	modules: {
		chooseColumn,
		loading,
		query,
		table
	}
}