import IsPROIcon from '../IsPROIcon';

const { __ } = wp.i18n;

export default {
	title: __( 'ConvertKit API', 'jet-form-builder' ),
	component: {
		name: 'convert-kit-tab',
	},
	disabled: true,
	icon: IsPROIcon,
};