import Tab from './PaymentsGateways.vue';

const { __ } = wp.i18n;

const title = __( 'Payments Gateways', 'jet-form-builder' );
const component = Tab;
const displayButton = false;

export {
	title,
	component,
	displayButton
}