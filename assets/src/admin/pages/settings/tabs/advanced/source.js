const { __ } = wp.i18n;

const label = {
	use_gateways: __( 'Use Gateways', 'jet-form-builder' ),
	enable_test_mode: __( 'Enable Test Mode', 'jet-form-builder' )
};

const help = {};

export { label, help };