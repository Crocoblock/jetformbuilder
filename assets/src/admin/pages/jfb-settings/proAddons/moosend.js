import IsPROIcon from '../IsPROIcon';

const { __ } = wp.i18n;

export default {
	title: __( 'Moosend API', 'jet-form-builder' ),
	component: {
		name: 'moosend',
	},
	disabled: true,
	icon: IsPROIcon,
};